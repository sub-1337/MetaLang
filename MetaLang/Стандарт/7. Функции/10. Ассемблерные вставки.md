В meta можно вставлять ассемлерные вставки.
В первых версиях планируется поддерживать x86, amd64, arm иснтрукции.

```
native fn mult()
{
    #asm.begin
    mov @eax, 10
    mov @ebx, 5
    mult
    #asm.end    
}
```